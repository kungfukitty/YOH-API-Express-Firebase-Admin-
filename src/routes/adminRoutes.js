import { Router } from 'express';import { requireAuth, requireRole } from '../middleware/auth.js';import { listRequests, approveRequest, declineRequest } from '../controllers/adminController.js';const r=Router();r.use(requireAuth, requireRole('admin'));r.get('/requests',listRequests);r.post('/requests/:id/approve',approveRequest);r.post('/requests/:id/decline',declineRequest);export default r;